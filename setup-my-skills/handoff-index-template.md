# Handoff — {PROJECT_NAME}

> 컨벤션: `AIL-handoff-topic-index`. topic 파일 1개당 인덱스 줄 1개 — 목록에 없으면 잊힌 것.
> 신규 topic: `docs/handoff/<slug>.md` 생성 + 아래 목록에 한 줄 추가(splice, 전체 재작성 금지).
> 형식: `- <slug> — <한줄 설명(BLUF)> — owner: <branch/agent 또는 없음> — <회수(merge) 여부·갱신일>`
> 우선순위(P0-P2)는 여기서 안 다룸 — issue tracker 몫.

---

## Topics

(없음)
